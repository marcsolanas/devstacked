---
# file: devstacked-hosts.yml
# VM config file for the Vagrant DevStack environment

hosts:
  devstack:
    memory: 2048
    ports:
      - guest: 3306 # MySQL
        host: 3306
        protocol: tcp
